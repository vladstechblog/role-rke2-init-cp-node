---
# defaults file for test_role
rke2_config_dir: /etc/rancher/rke2
rke2_config_name: config.yaml
rke2_config_dest: "{{ rke2_config_dir }}/{{ rke2_config_name }}"
rke2_server_svc: rke2-server.service
pod_cidr: 172.16.0.0/16
svc_cidr: 172.17.0.0/16
